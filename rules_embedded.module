<?php
/**
 * @file
 * Main module file.
 */

/**
 * Creates an instance of an embedded rule.
 */
function rules_embedded_rule($variables = array(), $provided_variables = array()) {
  return new RulesEmbeddedRule($variables, $provided_variables);
}
